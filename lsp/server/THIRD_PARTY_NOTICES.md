# Third-Party Notices

`clyth-lsp` is implemented using the Go standard library only and does not bundle third-party Go modules.

The Go toolchain and standard library are external build/runtime tooling and are not redistributed as source dependencies by this directory. See the Go project for its applicable licensing terms.
