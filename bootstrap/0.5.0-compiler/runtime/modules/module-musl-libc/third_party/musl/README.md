# Generated upstream attribution

`scripts/build_musl.sh` copies the exact `VERSION` and `COPYRIGHT` files from the
pinned musl 1.2.6 source checkout into this directory before generating compiler
license manifests. These files are deliberately not reconstructed from an older
Clyth contributor list.
