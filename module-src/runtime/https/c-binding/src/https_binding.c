/*
 * Clyth V1.0.0 HTTPS is a Clyth-space configuration facade over the router
 * transport. The router binding owns clyth_server_* native symbols; this
 * translation unit intentionally defines none so linking both modules cannot
 * create duplicate native implementations.
 */
