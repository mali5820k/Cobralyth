#ifndef concurrency_object_hpp
#define concurrency_object_hpp

// Atomic objects with custom locking API for extending duration/scope of interactibility to provide synchronization during multi-threaded reads and writes.

class Atomic_Object {
    Atomic_Object();
    ~Atomic_Object();

    //     
};

#endif