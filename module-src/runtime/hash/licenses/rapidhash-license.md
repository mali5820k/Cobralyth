# rapidhash dependency note

The Clyth hash binding is MIT licensed as part of Clyth. rapidhash is fetched dynamically at the exact commit pinned in the repository `third-party.lock`; its source is not vendored in Git.

The upstream MIT license is retained in `licenses/rapidhash-LICENSE` for source and binary package attribution.
