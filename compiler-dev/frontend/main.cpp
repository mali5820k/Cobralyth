#include "common.hpp"

int main(int argc, char** argv) {
    // Simply lex the strings from the file line-by-line.
    // std::string input_file = std::string(argv[1]);
    // if (std::filesystem::exists(input_file)) {

    // }
}