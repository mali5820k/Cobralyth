# Third-party dependency licenses

Clyth does not vendor these projects in the repository. Their source is fetched at build time at the exact commits recorded in `third-party.lock`.

The corresponding upstream license texts are retained here so source and binary distributions can reproduce required notices without first downloading the dependency source.
